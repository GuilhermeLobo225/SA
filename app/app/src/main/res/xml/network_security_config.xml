<?xml version="1.0" encoding="utf-8"?>
<!--
  A api.py (Flask) serve em HTTP simples. Desde a API 28 (Android 9), o sistema
  bloqueia tráfego cleartext por defeito. Aqui abrimos a exceção apenas para os
  hosts de desenvolvimento, mantendo tudo o resto seguro:
    - 10.0.2.2  → endereço que o emulador Android usa para chegar ao localhost do PC
    - localhost → para emuladores que façam port-forwarding
    - 192.168.* → IPs típicos de LAN para dispositivos físicos
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- ajustar este IP para o do PC quando se usa um dispositivo físico -->
        <domain includeSubdomains="true">192.168.1.0</domain>
        <domain includeSubdomains="true">172.20.10.2</domain>
        <domain includeSubdomains="true">172.20.10.6</domain>
    </domain-config>
</network-security-config>

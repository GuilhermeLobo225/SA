<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_page">

    <!-- ===== Toolbar (cabeçalho vermelho) ===== -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/uminho_red">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:title="@string/title_bibliotecas"
            app:subtitle="@string/subtitle_main"
            app:titleTextColor="@color/text_on_red"
            app:subtitleTextColor="@color/uminho_red_soft" />
    </com.google.android.material.appbar.AppBarLayout>

    <!-- ===== Conteúdo principal ===== -->
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipe_refresh"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/lista_bibliotecas"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingTop="8dp"
            android:paddingBottom="96dp"
            android:scrollbars="vertical"
            tools:listitem="@layout/item_biblioteca" />

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <!-- ===== Loading ===== -->
    <ProgressBar
        android:id="@+id/loading"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:indeterminateTint="@color/uminho_red"
        android:visibility="gone" />

    <!-- ===== FAB para abrir o catálogo ===== -->
    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
        android:id="@+id/fab_catalogo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="20dp"
        android:text="@string/ver_catalogo"
        android:textColor="@color/text_on_red"
        app:backgroundTint="@color/uminho_red"
        app:icon="@drawable/ic_search"
        app:iconTint="@color/text_on_red" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>

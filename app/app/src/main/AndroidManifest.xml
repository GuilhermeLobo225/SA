<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===== Permissões ===== -->
    <!-- Rede: precisamos de chamar a API local (processing/api.py) e
         eventualmente o Firebase. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Localização: necessárias para Geofencing (ver PL8).
         FINE -> precisa, COARSE -> aproximada (declaramos ambas como pede a Google),
         BACKGROUND -> só vale a pena pedir em API 29+. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Notificações: a partir de API 33 (Android 13) é runtime permission. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".SaApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SA"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="33">

        <!-- Activity principal: lista de bibliotecas -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity de detalhe (recebe ?id= via extra) -->
        <activity
            android:name=".ui.BibliotecaDetalheActivity"
            android:parentActivityName=".ui.MainActivity"
            android:exported="false" />

        <!-- Activity de pesquisa do catálogo -->
        <activity
            android:name=".ui.CatalogoActivity"
            android:parentActivityName=".ui.MainActivity"
            android:exported="false" />

        <!-- BroadcastReceiver para transições de geofence (PL8) -->
        <receiver
            android:name=".geofence.GeofenceBroadcastReceiver"
            android:enabled="true"
            android:exported="true" />

    </application>

</manifest>
